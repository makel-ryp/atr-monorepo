[build]
builder = "nixpacks"
buildCommand = "bun install && cd apps/inventory && bun run build"

[deploy]
startCommand = "bun apps/inventory/.output/server/index.mjs"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
